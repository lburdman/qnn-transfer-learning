# Core ML / audio
torch>=2.0
torchvision>=0.15
torchaudio>=2.0
pennylane>=0.38

# Audio features
librosa>=0.10
numpy<2          # Pin below 2.x: torch binary wheels require numpy 1.x ABI

# Data / analysis
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
Pillow>=10.0

# PANNs embeddings (optional — only needed for audio_preprocessing.ipynb PANNs section)
panns-inference

# Progress bars
tqdm

# Notebook environment
ipywidgets

# IBM Quantum (optional — only needed for hardware_verification.ipynb)
# qiskit>=1.0
# qiskit-ibm-runtime>=0.20
